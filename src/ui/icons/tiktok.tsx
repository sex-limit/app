import { type SvgProps } from 'react-native-svg';

import Tiktok from '@/ui/assets/icons/tiktok.svg';

export function TiktokIcon(props: SvgProps) {
  return <Tiktok {...props} color={'#fff'} width={16} height={16} />;
}
